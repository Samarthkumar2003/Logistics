[pytest]
testpaths = tests
addopts = -q --tb=short --strict-markers
markers =
    slow: takes more than a second

# Every test here runs offline: no Supabase, no Gmail, no LLM. If a test needs
# any of those, it belongs in an integration suite that is not this one.
